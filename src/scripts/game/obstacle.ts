export class Obstacle {
    positionX: number;
    trackIndex: number;

    constructor(positionX: number, trackIndex: number) {
        this.positionX = positionX;
        this.trackIndex = trackIndex;
    }
}
